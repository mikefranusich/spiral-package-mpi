# SPIRAL MPI package loader

ImportAll(fftx.library.cufft);

Include(tags);
Include(types);
Include(tspl);
Include(spl);
Include(nonterm);
Include(breakdown);
Include(code);
Include(rewrite);
Include(opts);
Include(api);

