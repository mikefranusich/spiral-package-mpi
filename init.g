# SPIRAL MPI package loader

#ImportAll(fftx.library.cufft);

Include(spl_gpu);
Include(tags);
Include(types);
Include(tspl);
Include(spl);
Include(nonterm);
Include(breakdown);
Include(code);
Include(rewrite);
Include(gpu_unparser);
Include(opts);
Include(api);

